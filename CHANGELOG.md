# Changelog

## v0.1.0

- Initialize standalone `copilot-subagent` skill packaging repo.
- Add local Bun workflows for build, verify, deploy, and local release tagging.
- Add distributable artifact generation for `.zip` and `.skill`.
